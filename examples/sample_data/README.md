# Sample CAAML Data

This directory contains a tiny SnowPilot/CAAML sample included in source
distributions. It is for parser smoke tests, documentation snippets, and release
checks.

The full research dataset remains in `examples/data/` in the git repository and
is intentionally not packaged into source distributions.
